name: Functions CI

on:
  workflow_call:
  push:
    branches:
      - main
      - feature/*
    paths:
      - 'functions/**'
  pull_request:
    branches:
      - main
    paths:
      - 'functions/**'

concurrency:
  group: ${{ github.ref }}-functions-ci
  cancel-in-progress: true

jobs:
  lint:
    uses: ./.github/workflows/ruff.yaml

  functions-tests:
    runs-on: ubuntu-latest
    needs: [lint]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: 'latest'

      - name: Sync deps (pyproject-first)
        run: |
          uv venv
          ./scripts/uv-sync.sh --all

      - name: Run pytest
        working-directory: ./functions/src
        run: |
          ../.venv/bin/python -m pytest
